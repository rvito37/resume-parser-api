# Choose one: openai or anthropic
AI_PROVIDER=openai

# OpenAI API Key (if using openai)
OPENAI_API_KEY=sk-your-key-here

# Anthropic API Key (if using anthropic)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Server settings
HOST=0.0.0.0
PORT=8000

# Redis (Docker Compose sets this automatically)
REDIS_URL=redis://localhost:6379/0

# Environment: development or production
ENVIRONMENT=development

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# CORS origins (comma-separated, or * for all)
CORS_ORIGINS=*

# RapidAPI (optional - set when publishing to RapidAPI)
RAPIDAPI_PROXY_SECRET=

# API Keys for your customers (comma-separated)
# Format: key:tier (tier = free, pro, ultra, mega)
API_KEYS=demo-key-123:free
